# 📘 دليل مشروع Project Risk AI (بالتفاصيل)

أهلاً بيكي! 👋 ده دليل شامل هيشرحلك كل حاجة عملناها، الكود شغال إزاي، وإزاي تشغليه عندك خطوة خطوة.

---

## 🏗️ 1. هيكلية المشروع (Project Structure)
إحنا رتبنا الملفات بشكل احترافي عشان نتجنب أي فوضى. الفولدر عندك دلوقتي المفروض يكون شكله كده:

```text
project_risk_ai/
│
├── data/                    <-- مكان ملف الـ CSV
│   └── project_risk_raw_dataset.csv
│
├── models/                  <-- الكود هيحفظ الموديل هنا
│   ├── risk_model.pkl
│   └── train_columns.pkl    <-- ملف مهم جداً فيه أسماء الأعمدة
│
├── train_model.py           <-- كود تدريب الموديل
├── app.py                   <-- كود الشات بوت (Streamlit)
└── requirements.txt         <-- ملف فيه كل المكتبات المطلوبة
```

---

## 🛠️ 2. شرح الكود (Logic)

### 🅰️ كود التدريب (`train_model.py`)
وظيفته: ياخد الداتا، ينظفها، يعلم الموديل، ويحفظ "عقل" الموديل في ملف.

1.  **Loading Data**: بنقرأ ملف الـ CSV.
2.  **Cleaning**:
    *   الأرقام الناقصة ⬅️ بنملاها بالـ Median (الوسيط).
    *   الكلام الناقص ⬅️ بنملاه بالـ Mode (الأكثر تكراراً).
3.  **Feature Engineering (مهم جداً 🧠)**:
    *   بنحسب حاجات جديدة ذكية، زي `Budget_per_Month` (الميزانية الشهرية) و `Workload_Index` (مؤشر ضغط العمل).
    *   ده بيخلي الموديل أذكى بكتير من إنه ياخد الأرقام الخام بس.
4.  **Training**:
    *   استخدمنا تقنية قوية اسمها **Stacking**.
    *   يعني جبنا موديل (Random Forest) وموديل (XGBoost) وخليناهم يشتغلوا سوا، وحطينا فوقهم "مدير" (Logistic Regression) ياخد أحسن قرار منهم.
5.  **Saving (السر هنا 🔑)**:
    *   حفظنا الموديل `risk_model.pkl`.
    *   **الأهم**: حفظنا أسماء الأعمدة في `train_columns.pkl`. ليه؟ عشان لما نيجي في التطبيق، نتأكد إننا بنبعت نفس ترتيب وشكل الداتا للموديل بالظبط، فمايحصلش Error.

### 🅱️ كود التطبيق (`app.py`)
وظيفته: ياخد كلام المستخدم، يجهزه، ويسأل الموديل.

1.  **Form Input**: بدل ما نكتب نص ونحاول نفكه (Parsing Errors)، عملنا "فورم" جاهزة بأزرار ومربعات، ده أسهل وأضمن بكتير.
2.  **Processing**:
    *   بنحول مدخلات المستخدم لجدول (DataFrame).
    *   **بنطبق نفس معادلات Feature Engineering** اللي عملناها في التدريب بالظبط.
3.  **The Fix (Reindex) 🪄**:
    *   بنحمل ملف `train_columns.pkl`.
    *   بنجبر جدول المستخدم إنه يكون له نفس الأعمدة بالظبط. لو فيه عمود ناقص بنحط مكانه صفر.
    *   ده اللي حل مشكلة الـ Crash القديمة.

---

## 🚀 3. إزاي تشغلي المشروع (خطوة بخطوة)

### الخطوة 1: تجهيز الفولدر
1.  انقلي ملف `project_risk_raw_dataset.csv` بتاعك وحطيه جوه فولدر اسمه `data` داخل فولدر المشروع.

### الخطوة 2: تشغيل التدريب
افتحي الـ terminal (أو CMD) جوه الفولدر واكتبي:

```bash
python train_model.py
```
*   هتلاقي كلام كتير بيظهر (Cleaning... Training...).
*   في الآخر هيقولك: `🎉 DONE!`.
*   هتلاقي فولدر `models` ظهر وفيه ملفين جداد.

### الخطوة 3: تشغيل التطبيق
اكتبي في الـ terminal:

```bash
streamlit run app.py
```
*   هيفتحلك صفحة ويب فيها البرنامج.
*   جربي تدخلي أرقام وتدوسي "Predict Risk".

---

## 📦 4. المكتبات (`requirements.txt`)
ده الملف اللي فيه أسماء الأدوات اللي استخدمناها. عشان تنزليهم كلهم مرة واحدة، اكتبي:

```bash
pip install -r requirements.txt
```

---
بالتوفيق! لو وقف معاكي أي حاجة تانية قوليلي. 💪
